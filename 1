3. نستخدم "تريغر" (وظيفة تلقائية) بحيث:
عند كل تسجيل مستخدم جديد في auth.users، يتم إنشاء صف في public.users تلقائيًا.

ويُولّد له كود إحالة تلقائيًا.

أي شخص يسجل بحساب Google يتم تسجيله تلقائيًا في public.users
